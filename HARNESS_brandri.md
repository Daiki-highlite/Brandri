# Brandri 記事ハーネス — 全体像

> Brandri の記事に「Highlite の視点」を必ず載せるための仕組みの見取り図。
> 「ハーネス」は単一のプロンプトファイルではなく、**手順書 → 発注書 → 投入スクリプト → ビルド時バリデータ** の4層で成立している。
> 作成日：2026-08-17 ／ 対象リポジトリ：`Brandri`

---

## 0. 要点

- 記事の主役は **元ニュースではなく Highlite の解釈**。元記事は末尾の出典ボックスから辿れる参照に徹する。
- 「Highlite の視点」は**任意の飾りではなく構造上の必須要素**。entries では欠けるとビルドが落ち、news では末尾の `Brandri Point` ボックスに自動で昇格する。
- トーン・禁止語・字数は**文書（ROUTINE / SPEC）で規範を示し、スクリプトで機械的に検証する**二段構え。人間の努力目標にしていない。

---

## 1. 4層の構造

| 層 | ファイル | 役割 |
|---|---|---|
| ① 手順書 | `ROUTINE.md` | 週2回のニュースルーティン。実質的なシステムプロンプト |
| ② 発注書 | `SPEC_KEYWORD_ARTICLES.md`／`SPEC_DESIGNER_TRACK/`／`SPEC_BASICS_PAGES.md` | トラック別の記事ハーネスと禁止語 |
| ③ 投入口 | `scripts/pending-news.mjs`／`scripts/write-article.mjs` | 巨大な `news.json` を開かずに読み書きする経路。書き込み前検証 |
| ④ 強制層 | `scripts/build-data.mjs` | 必須章の検証・公開ゲート・視点節の自動レイアウト |

```
update-news.mjs   →  pending-news.mjs  →  （執筆）  →  write-article.mjs  →  build-data.mjs  →  commit/push
（RSS取り込み）      （未執筆の一覧）                （検証つき投入）       （生成・公開ゲート）    （Actions→Xserver）
```

---

## 2. 第①層：`ROUTINE.md` — ニュースルーティンの手順書

月曜・木曜の週2回、1回1本。取り込み本数は `project/data/news.json` の `policy.perDay`（現在 `1`、保持 `keep: 21` のローリング）で決まる。

### 2-1. 大原則（`ROUTINE.md:13-18`）

- **ニュースを"そのまま"取り上げない。** 表示される `headline` と本文は、ニュースから Brandri が導き出した示唆にする
- **必ずポジティブに書く。** 取り上げる企業・取り組みへの敬意を保ち、批判・不安を煽る切り口にしない
- 課題を扱うときも「ここから何を学べるか」「どう活かせるか」の前向きな視点で書く
- 未執筆の生データはサイト全体から非公開になる（露出しない）

### 2-2. 記事ハーネス（`ROUTINE.md:61-91`）

本文（sections 合計）**800〜1600字**。構成は固定。

| フィールド | 内容 | 目安 |
|---|---|---|
| `headline` | Brandri独自の見出し＝ニュースから導いた「気づき・結論・提案・共感」。元記事見出しの流用不可。断定・問い・逆説を込める | 1行 |
| `insight` | 標題リード（standfirst）。ニュースを一文のブランディング論点に翻訳 | 60〜90字 |
| `sections[0]` | **01 ニュースの概要** — 事実（誰が・何を・どうした）を自分の言葉で要約＋元記事の切り口を一言（「〜と報じている」等で引用を明示） | 250〜400字 |
| `sections[1]` | **02 気づき／示唆** — 引き出せるブランディングの示唆。提案 or 共感を前向きな学びとして | 350〜700字 |
| `sections[終盤]` | **「と、いうことで。」**（見出し文言まで全記事統一）— 読後の最初の一歩を一つ名指しする（測る／決める／棚卸しする 等） | 250〜400字 |
| `sections[末尾]` | **「Brandriの視点：〜」** — 見出しが `Brandri`/`Highlite` で始まる節は末尾の `Brandri Point` に自動変換される | 200〜350字 |
| `pullquote` | Brandriの立場を一文で（`Brandri Point` 内に自動挿入） | 1文 |
| `takeaways` | 「経営がここから判断すべきこと」2〜4項目 | 各40字前後 |
| `aside` | 人間味のある口語のひとこと（記事全体の約5%）。最初の節の直後に自動挿入 | 50〜100字 |

- 各 `sections[n]` は `{ "num": "01", "h": "見出し", "p": ["段落", …] }`
- **01は"要約"、02–03が"主役"。** 概要は事実紹介に徹し、価値判断は02以降（役割を混ぜない）
- 強調は本文中の `<em>…</em>`。見出し・`pullquote` にタグは入れない

### 2-3. 編集スタンス・語彙（`ROUTINE.md:47-55`）

- 断定を恐れず、立場を一文で宣言する（両論あるときは両論に触れてから）
- 語彙を Brandri の思想に揃える：**「判断の一貫性」「見た目の手前」「フェーズ」「判断基準としてのブランド」**
- 煽らない。「〜すべき」の乱発より **「分かれ目」「論点」の提示**を優先
- `cat` は 定義論 / AI時代 / フェーズ別 / インナー / 採用 / 計測 / リブランド / 運用 / 経営 / ニュース から選ぶ
- 書き終えたら **`stop-ai-slop-jp` スキルで AI臭を落とす**（→ §9）。Brandri 規範が優先する4点は §9-2、毒と皮肉の線引きは §9-3

### 2-4. トーン規範（`ROUTINE.md:83-84`）

- 取り上げる企業・人物を批判しない
- 失敗事例に見えるニュースも「そこから引き出せる学び」を主役にし、希望や打ち手が残る終わり方にする
- **煽りタイトル禁止**（「〜の落とし穴」「危険な〜」等）

### 2-5. 口語アサイド規範（`ROUTINE.md:78-81`）

記事全体は「だ・である」調のまま、`aside` の一文だけ一段くだけた話し言葉にして"人が書いた温度"を出す。

- その記事の主題に必ず結びつけ、使い回さない（記事ごとに固有）
- 新情報を足さず、本文の要点への共感・本音・現場のあるあるを述べる
- 語り口の例：「〜なんですよね。」「正直、〜」「じつは〜」「〜な気がします。」「個人的には〜」
- **禁止**：スラング（めっちゃ／ヤバい等）・絵文字・「！」乱用・固有名詞や数値の創作・英字の全大文字

### 2-6. 著作権の絶対規範（`ROUTINE.md:122-127`）

- **元記事の本文・文章を転載しない。** 使ってよいのは「見出し（事実）」と「リンク」だけ
- `sections` は必ず自分の言葉によるブランディング観点の分析。ニュースは"起点"であり、**記事の主役は Highlite の解釈**
- 事実として確認できない固有名詞・数値を創作しない。不確かなら一般論に留める
- 引用元は詳細記事末尾の `news-source-box` に `rel="nofollow"` の外部リンクとして自動で出る（唯一の導線）

### 2-7. 禁止事項（`ROUTINE.md:164-169`）

- `project/data/news.json` を直接 Read / Edit しない（`pending-news.mjs` で読み、`write-article.mjs` で書く）
- `news.json` 以外のデータファイル（articles / cases / site）を変更しない
- 既存アイテムの `insight` を書き換えない（当日追加分のみ執筆）
- ニュース元の記事本文を転載しない
- **英字の全大文字表記を使わない（ブランド毀損）** — `Highlite`（HIGHLITE不可）、`Brandri`（BRANDRI不可）。装飾ラベルも `Sources` `News` のように頭文字のみ大文字。頭字語（AI／MVV／PMF等）と他社の公式表記のみ例外

### 2-8. サムネイル規範（`ROUTINE.md:129-138`）

1200×750 / 抽象のみ（文字・ロゴ・具象イラスト禁止）。パレットはミネラル系に限定：
`#1E2340` `#3D5070` `#9B8CC8` `#7BBAD4` `#C8A4C4` `#8CC4D0` ＋ 紙色 `rgba(250,246,236,…)`

`gen-thumb.mjs` がタイトルのキーワード→モチーフを自動選択（経営/拡張→柱、AI→ノード網、採用→求心、リブランド→積層した紙、体験→同心の波、調査/売上→上昇する棒、デザイン→重なる三角）。手描きで上書きする場合もこの対応関係を踏襲する。

---

## 3. 第②層：トラック別の発注書

### 3-1. 経営者トラック — `SPEC_KEYWORD_ARTICLES.md`

L1語（経営者の痛み語）30本。**禁止語リストの本体はここ。**

**大原則（`:12-18`）**

1. **タイトルと前半に「ブランディング」を出さない。** 見出しは経営者の痛み語だけ。「ブランディング」は第03章で初出
2. **痛みが先、詩は後。** 経営者の実語彙（値決め・粗利・応募が来ない・属人化…）で捕まえてから、Brandri の思想語で格を上げる
3. **ゴールでなく仕組みを語る。**「有名になる」ではなく「相見積もりに呼ばれなくなる構造」
4. 事実の創作禁止。引用は `sources` に出典明記
5. 煽らない（「〜すべき」より「分かれ目」「論点」）

**捨てる言葉＝使用禁止（`:15`）**

> 世界観／トンマナ／クリエイティブ／アートディレクション／パーパス／ナラティブ／エンゲージメント／バズる／認知度アップ／フォロワー／映え／おしゃれ・かっこいい・素敵／デザイン思考／DX／イノベーション

- 「リブランディング」は**見出し禁止**（本文では「作り直す」「定義し直す」）
- 「MVV」は用語ページへのリンク文脈のみ可
- 「CACの高騰」等のマーケ語も使わない（`:209`）

**構成（`:20-35`）** — 1,800〜2,400字

| 章 | 役割 | 目安 |
|---|---|---|
| `title` | L1痛み語だけの見出し。問い・対比・言い切り | 1行 |
| `lead` | 経営者の独白から始める。「ブランディング」不使用 | 80〜120字 |
| **01 症状** | 痛みの言語化。「うちのことだ」と思う場面描写 | 400〜500字 |
| **02 構造** | なぜ起きるか。"選ばれ方の構造"の問題として、まだブランディングと言わずに解剖 | 450〜550字 |
| **03 解の名前** | ここで初めて翻訳する——「それを整える営みを、ブランディングと呼ぶ」 | 450〜550字 |
| **04 明日の一歩** | 実務の初手＋内部導線 | 350〜450字 |

内部導線は各記事2〜3本必須（診断／5大疑問／状況ページ／entries）。

### 3-2. デザイナートラック — `SPEC_DESIGNER_TRACK/00_MASTER.md` ＋ `01`〜`09`

**経営者トラックとの決定的な差分（`:22-28`）**

- **経営者トラックの禁止語（トンマナ／クリエイティブ／デザイン思考等）は本トラックでは解除。** デザイナーの実語彙で書く（ただし乱用せず glossary へリンク）
- 共通で厳守：英字全大文字禁止（AI・VI・CI・UI・UX・SNS・LP は例外）／事実・数値・引用の創作禁止／煽らない
- トーン：**先輩デザイナーが隣で話す距離感。**「べき」より「分かれ目」。経営視点は説教にせず「単価と裁量が上がる話」として扱う

**登らせる階段（`:13-17`）**

```
手法（作り方・型）→ 原理（良し悪しを分ける観点）→ ブランド（コアから逆算する）→ 経営（なぜ利益に効くか）
```

**構成（`:48-62`）** — 2,000〜2,600字。01 制作の型 → 02 良し悪しの分かれ目 → **03 ブランドから逆算する（全記事必須）** → 04 AIとの分業／経営にどう効くか。

### 3-3. 5大疑問ページ — `SPEC_BASICS_PAGES.md`

構成テンプレート（結論→図解→本文→タブ/Q&A→次へ）中心のデザイン仕様。編集規範としては `:44` の「英字はタイトルケースのみ（全大文字禁止）」が共通項。

---

## 4. 第③層：投入スクリプト

### 4-1. `scripts/pending-news.mjs` — 読む側の入口

`news.json` 全体を読まずに済ませるための軽量化。**未執筆アイテムの `id` / `date` / `cat` / `title` / `source` / `thumb` だけ**を出す（既存本文は載せない）。

```bash
node scripts/pending-news.mjs            # 未執筆一覧
node scripts/pending-news.mjs --json     # 機械可読
node scripts/pending-news.mjs --id <id>  # 特定IDの現在値
node scripts/pending-news.mjs --sample   # 直近の執筆済み1本を手本として出力
```

`--sample` は「常に最新の執筆済み1本」を動的に出す。保持が21件ローリングのため、**特定IDを手本として文書に固定しない**設計。

### 4-2. `scripts/write-article.mjs` — 書く側の入口

1記事分の小さな JSON だけ書けばよくする投入口。

```bash
node scripts/write-article.mjs <id> <本文JSONのパス> [--force]
node scripts/write-article.mjs <id> -              # 標準入力
```

**書き込み前検証（不備があれば `news.json` は一切変更されない）**

- `headline` / `insight` / `sections` の存在
- `sections` が配列で、各要素に `h` と段落配列 `p` があること
- 見出し `h` と `pullquote` にタグが入っていないこと
- `takeaways` が配列であること
- 合計字数（`build-data.mjs` と同一計算）：800未満／1800超で警告

**安全装置**

- 執筆済みアイテムは誤爆防止で拒否（`--force` のみ例外）
- 書き込むのは `headline` `insight` `sections` `pullquote` `takeaways` `aside` `cat` のみ。`id` `date` `title` `source` `thumb` `color` `pattern` は触らない
- 該当アイテムのキー順を正規化し、git diff が無用に膨らむのを防ぐ

### 4-3. `scripts/update-news.mjs` — 取り込み

Google News RSS（6クエリ・`when:14d`）から候補を取得し、**媒体名の `ALLOWLIST`** で絞る。

- クエリ：ブランディング 経営／ブランド戦略／リブランディング／コーポレート・インナーブランディング／ブランド マーケティング 戦略／ブランド体験・顧客体験 デザイン
- 許可媒体：宣伝会議・AdverTimes・ブレーン・広報会議 等の広告PR専門／日経・東洋経済・ダイヤモンド・Forbes 等の経営総合／MarkeZine・Web担・ITmedia 等のマーケ専門／AXIS・JDN・MdN 等のデザイン／電通・博報堂・インテージ 等の調査
- ファッション/エンタメ/純アグリゲータは除外。**該当が無い日は本数が減ってよい（無理に足さない）**

---

## 5. 第④層：`scripts/build-data.mjs` — 機械的な強制

> 2034行。日本語混在で `grep` に binary 判定されるため、検索時は `grep -a` を使う。

### 5-1. Highlite の視点を必須にしている箇所

**entries（課題／フェーズ／用語ページ）は4つの必須章（`:131-145`）**

| 必須章 | フィールド |
|---|---|
| ブランドのナレッジ | `knowledge.paras` |
| 課題解決方法例 | `solutions.steps` |
| 他社事例 | `cases.works` |
| **Highliteの観点** | **`highlite.view`** |

```js
if (!e.highlite || !Array.isArray(e.highlite.view) || !e.highlite.view.length)
  fail(`entries[${i}] (${e.slug}) に highlite（Highliteの観点）がありません — 必須章です`);
```

`fail()` はビルドを止める。**視点が無い入口ページは存在できない。**

データ側の形（`project/data/entries.json`）：

```json
"highlite": {
  "view": ["私たちは、値引き圧力を「顧客からの質問」だと捉えている。…"],
  "pull": "値引き要求とは「高く買う理由を教えてほしい」という質問である。"
}
```

### 5-2. 公開ゲート（`:227-234`）

```js
// Brandri独自の headline と本文 sections が揃った記事だけを公開する。
// 生データ（他社見出しのまま・本文未執筆）はサイトのどこにも露出させない。
```

未執筆の記事は §00 にもジャーナルにも検索にも sitemap にも出ない。**取り込んだ日は必ず執筆まで完了させる必要がある。**

### 5-3. 視点節の自動レイアウト（`:718-757`）

```js
const isViewpoint = (h) => /^(Brandri|Highlite)/.test(h || "");
```

- 見出しが `Brandri` / `Highlite` で始まる節を本文から**抜き出し**、記事末尾の `<aside class="brandri-point">` へ移設
- 見出しから接頭辞（`の視点` `が見る` `が感じる` `View` `Point` ＋区切り記号）を削って `bp-title` にする
- `pullquote` はそのボックス内の `bp-stance` として自動挿入
- 視点節が無い場合のみ、従来のプルクオート＋`— Brandri / Highlite editorial` にフォールバック
- `aside`（口語のひとこと）は最初の節の直後に `<p class="human-aside">` として自動挿入

### 5-4. その他の視点表示

| 箇所 | 表示 |
|---|---|
| 読み物記事（`:1050-1053`） | `pullquote` を「▸ Highlite の見立て」注釈ボックスに |
| entries（`:1352-1353`） | `highlite.view` を段落展開＋`highlite.pull` を `— Brandri / Highlite editorial` 付きプルクオートに |
| 5大疑問（`:1571`） | `b.pull` を `highlite-note` に |
| JSON-LD（`:506-509`） | `author`/`publisher` = `Highlite Inc.`、`insight` を `Brandri (Highlite Inc.)` の `Comment` として構造化 |

### 5-5. 記事字数・出典の検証

- articles：`sources` 必須（`:100`「出典なき記事は公開できません」）、`slug` 一意、`sections` 3節以上
- news：`source.name` / `source.url` 必須（`:205`「出典なきニュースは掲載できません」）、同一URL重複でビルド失敗、字数800未満/1800超で警告

---

## 6. 対外的な視点の宣言

| ファイル | 内容 |
|---|---|
| `project/llms.txt` | AI向けの定義文。「編集・運営は、ブランディング支援会社 Highlite Inc.」「ブランディングの実務相談は Highlite へ」 |
| `project/data/site.json` | `meta.publisher: "Highlite Inc."`、課題9種・フェーズ7種の語彙定義 |
| `build-data.mjs:71` | `CONTACT_URL = "https://highlite.co.jp/contact/"` — 全ページのCTA |

---

## 7. 実行手順（要約）

```bash
node scripts/update-news.mjs      # 1. RSS取り込み（許可媒体のみ・perDay本）
node scripts/pending-news.mjs     # 2. 未執筆の確認
# → 記事1本ごとに本文JSONを書く
node scripts/write-article.mjs <id> <path>   # 2.5 検証つき投入
# 3. サムネを必要に応じて上書き（project/assets/thumbs/<id>.svg）
node scripts/build-data.mjs       # 4. 再ビルド。警告が残ったまま進まない
# 5. 生成物を全部 add してコミット・push（Actions→Xserver）
```

`build-data.mjs` は news 以外の静的ページ（articles / entries / glossary 等）も再生成するため、**コミット時は生成物を全部 add する**。絞ると翌回の `git pull` で衝突しかける。

---

## 8. 補足・注意点

- **`README.md` はこのリポジトリの実態と合っていない。** Claude Design のハンドオフ用汎用文で、「`brandri/project/Brandri.html` を読め」と書いてあるが該当ファイルは無い。実態は静的サイト＋自動更新ルーティン
- `project/articles/ai-brand-design-harness.html` は「ハーネス」を論じた**記事コンテンツ**であり、生成ハーネス本体ではない
- `SPEC_KEYWORD_ARTICLES.md:5` が参照する元資料『Highlite_広告語彙辞典』はリポジトリ外（別マシンのローカルパス）にあり、追跡されていない
- 別系統として、`musickappa/highlite-design-system` の `branch_yuto` に Python + `.claude/agents` 構成の Brandri 記事パイプライン（Slack連携・日次）が存在する。本リポジトリの Node 系ハーネスとは独立

---

## 9. AI臭の除去 — `stop-ai-slop-jp` / `stop-slop` スキル

記事本文（`sections[].p` / `lead` / `pullquote` / `aside`）を書いたら、**書き終わった直後に必ず1回通す**。
新規執筆だけでなく、既存記事の加筆・リライトでも同じ。

| スキル | 置き場所 | 使う場面 |
|---|---|---|
| `stop-ai-slop-jp` | `.claude/skills/stop-ai-slop-jp/` | **日本語の記事本文すべて（主）** |
| `stop-slop` | `.claude/skills/stop-slop/` | 英文（`llms.txt` の英語部・英語メタ）を書くとき |

出典：`iKora128/stop-ai-slop-jp`（MIT, Daichi Nagashima）/ `hardikpandya/stop-slop`（MIT, Hardik Pandya）。
どちらも上流をそのまま vendoring している。**`SKILL.md` と `references/` を直接編集しない**（上流の更新を取り込めなくなる）。
Brandri 固有の調整は、下の「9-2」に書き足す。

### 9-1. そのまま効くルール（Brandri と衝突しない）

- **false agency を潰す** — 「データが示している」「文化が醸成される」を、誰が何をしたかに書き換える
- **具体に降りる** — 「重要だ」「本質的だ」で終わらせない。固有名詞・数値・場面に落とす
- **翻訳調動詞を減らす** — 「指している」「示している」「物語っている」「収斂する」
- **横文字メタファーを日本語に戻す** — 「思考のOSをアップデート」「習慣をインストール」の類
- **形容詞・心情を「」で囲まない** — 固有名詞と直接引用以外で使わない
- **アカデミック自称を使わない** — 地の文の「本稿」「本記事」「筆者」
- **リズムにムラを入れる** — 全段落を同じ長さ・同じ強度で着地させない
- **偏愛語を撒かない** — 「手触り」「解像度」「本質」「営み」「文脈」を1記事に複数置かない

### 9-2. Brandri 規範が優先する箇所（スキルに従わない）

スキルは「個人ブログの一人称」を想定している。Brandri は媒体なので、次の4点は**Brandri 側が勝つ**。

1. **断定は弱めない。** スキルは「伝聞を残す」「強度を一段下げる」と言うが、
   `ROUTINE.md:49` の**「断定を恐れず、立場を一文で宣言する」**が優先。ヘッジで薄めない。
   ただし**事実の記述**（調べた数値・他社の動き）に伝聞を残すのは、スキル側が正しい。両者は両立する。
2. **中間温度語（「まあまあ」「微妙」「よく分からん」）は `aside` の中だけ。** 地の文は「だ・である」調を保つ。
   スラング（めっちゃ／ヤバい）は `aside` でも禁止（`ROUTINE.md:78-81`）。
3. **両論併記は捨てない。** スキルは「全方位肯定は判断の放棄」と言うが、Brandri の型は
   **「両論に触れてから立場を宣言する」**。触れずに切り捨てるのではなく、触れた上で降りる。
4. **「3項目並列を疑う」は構造要件に負ける。** `sections` 3節以上・`related` 3本は
   `build-data.mjs` と `write-knowledge.mjs` が機械的に検証している。ここは削らない。
   スキルの指摘が効くのは**本文中の「3つのポイント」列挙**であって、記事の構造ではない。

### 9-3. 毒と皮肉の線引き（条件つきでスキルを採用）

スキルの「毒を許す」は**条件つきで採る**。人間味が残るので、全部を削り落とさない。
ただし向ける先と強度に線を引く。

**入れてよい**

- 軽めのジョーク、業界の"あるある"への皮肉
- 自虐（Highlite / Brandri 自身、書き手自身に向けたもの）
- 一般論としての苦言（「この手の施策はだいたい途中で止まる」など、対象を名指さないもの）

**入れない**

- **Fワード・それに類する語**、および**直接的な罵倒**（対象が誰であれ）
- **取り上げた企業・人物への批判**（`ROUTINE.md:83-84` のトーン規範は生きている）。
  皮肉の矛先は、個社ではなく状況・構造・自分に向ける
- 煽りタイトル（`ROUTINE.md:83-84`）。本文で皮肉を効かせても、見出しでは煽らない

**判断に迷ったら残して出す。** 強すぎるものはレビューで削ぎ落とす運用にする
（2026-08-31 ユーザー判断）。書く時点で自己検閲して無味乾燥にするより、出してから削る方を採る。
### 9-4. 全角ダッシュ `—` の扱い

スキルは全角ダッシュの全廃を求めるが、Brandri では既に **44記事・272箇所**で使われており、
`Brandri — ここだけでわかるブランドの全て` のようなラベル区切りは表記の一部になっている。運用は分ける。

- **段落の地の文（`p`）** — 新規記事では使わない。読点・コロン・改行に置き換える
- **タイトル・見出し・ラベルの区切り** — 従来どおり残す（`構造——「看板」とは…` のような既存の型を含む）
- **既存44記事の遡及修正はしない。** リライトでその段落に触れたときだけ直す

### 9-5. 手順のどこに入るか

`ROUTINE.md` の §2.5（詳細記事の執筆）と、トラック別記事の執筆の**両方**で、
JSON を `write-article.mjs` / `write-knowledge.mjs` に渡す**前**に通す。
スクリプトの検証（必須項目・字数・出典）は AI臭を見ないので、ここは人と Claude 側の責任になる。
