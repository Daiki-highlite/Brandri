# Spec — 「ブランディング、まずはここから。」5大疑問ページの精緻化

> 目的：basics/ 配下の5記事を「単純なweb記事」から、図解・タブ・アコーディオンを備えた**精緻な固定ページ**へ再設計する。
> 前提：5ページとも**同一の構成テンプレート**を使う（中身のデータだけ差し替え）。
> 画像・図解は**すべて後から差し替え可能**な規約で実装する（§6）。
> 作成日：2026-07-05 ／ 対象：project/basics/*.html（build-data.mjs が basics.json から生成）

---

## 1. ページ構成（全5ページ共通・上から順）

| # | ブロック | 内容 | デザイン指定 |
|---|---|---|---|
| 0 | **プログレスバー** | ページ上端に読了プログレス（スクロール連動、高さ3px） | crystal-purple #9B8CC8。JSなしでも害なし（装飾） |
| 1 | **ヒーロー** | パンくず（Brandri › まずはここから › 疑問N）／大きな番号 01–05／疑問文をserifの大見出し／リード（standfirst）／メタ行（読了目安・更新日）／右側にアイコン | ダークネイビー #1E2340 帯（トップの basics セクションと同トーン）。アイコンは assets/basics/`slug`.svg を流用（差し替え可） |
| 2 | **30秒サマリー**（結論ファースト） | 「先に、答え。」ラベル＋**一文の答え**（既存 pull を昇格）＋要点3つの箇条書き | 紙色カードに crystal-purple の左罫。mono ラベル「Answer / 30秒でわかる」 |
| 3 | **ページ内目次** | 本文4節＋タブ＋Q&Aへのアンカー | PC：本文左に sticky 縦目次。SP：ヘッダー直下に横スクロールのチップ列（sticky、高さ48px） |
| 4 | **メイン図解（fig1）** | その疑問の核心を1枚で表す図 | 全幅 figure。白カード＋細罫、下にキャプション（mono・11px）。**差し替え可能画像**（§6） |
| 5 | **本文 4節** | 既存 basics.json の sections（01–04 の節番号付き）。第2節の後に pullquote | 記事本文は ivory #EBEBEB 背景・serif見出し・番号は mono。既存 news-article のトーンを踏襲 |
| 6 | **サブ図解（fig2）** | 本文第3節の後に挿入する補助図 | fig1 と同コンポーネント。**差し替え可能** |
| 7 | **タブ切り替え** | 「視点を切り替えて見る」ブロック。ページごとにタブ軸が違う（§4） | セグメントコントロール型。mono ラベル、アクティブは下線＋crystal-purple。SP：横スクロール可のタブ列。**JSオフ時は縦に全展開**（プログレッシブエンハンスメント） |
| 8 | **アコーディオンQ&A** | 「よくある誤解」4–6問 | `<details>/<summary>` ベース（JS不要で動作）。＋アイコンが45°回転。開いた項目は左罫 crystal-purple |
| 9 | **ミニ用語辞典** | 関連する entries/term-*.html への用語チップ 3–5個 | 小さな角丸チップ。既存 entry ページへの内部リンク |
| 10 | **前後ナビ** | 「前の疑問 / 次の疑問」ペア＋5点ドットインジケーター | カード2枚（SP縦積み）。05の次は診断へ誘導 |
| 11 | **チェックCTA** | 「自社のブランドを、2分でチェックする」 | 既存 .basics-check-cta（白グラデ＋菱形矢印）をそのまま再利用 |
| 12 | **フッター** | 既存共通フッター | 変更なし |

### 読み味の設計
- **結論→図解→本文→操作（タブ/Q&A）→次へ** の順。忙しい読者は §2 と fig1 だけで要旨が取れる。
- インタラクションは「触ると理解が深まる」ものに限定し、装飾目的のJSは入れない。

## 2. デザイントークン（既存に揃える）
- 色：ネイビー #1E2340／紙 #F4F4F4／アイボリー #EBEBEB／アクセント #9B8CC8／補助 #7BBAD4・#3D5070
- 書体：見出し=var(--serif)、ラベル・番号=var(--mono)、本文=既存本文書体
- 余白：セクション間 PC 96px / SP 64px。本文最大幅 720px、図解最大幅 960px
- 英字は**タイトルケースのみ**（全大文字禁止。Answer / Q&A は例外的頭字語のみ）

## 3. スマホレイアウト（〜760px）
- ヒーロー：番号とアイコンを横並び→縦積み、見出し clamp(26px, 7vw, 34px)
- 目次：横スクロールチップ（sticky）。タップで smooth scroll
- 図解：左右フルブリード（padding 16px）。図が横長の場合は `overflow-x:auto` で図内スクロール
- タブ：タブ列は横スクロール、パネルは全幅。タップ領域は高さ44px以上
- アコーディオン：summary の行高 52px、開閉アイコンは右端固定
- 前後ナビ：縦積み。CTAは通常フロー（固定追従はしない）

## 4. ページ別の中身プラン（図解・タブ・Q&Aの設計）

### 01 what-is-brand｜結局、ブランドって何？
- fig1：**「像は相手の頭の中に結ばれる」**——会社→接点（ロゴ・接客・商品・対応）→頭の中の期待の像、の流れ図
- fig2：**接点の総和**——一箇所の綻びで像が崩れる図（積み木/天秤モチーフ）
- タブ軸：**立場で見る**（経営者から／社員から／顧客から、それぞれブランドがどう見えるか）
- Q&A：ロゴを作ればブランド？／大企業だけの話？／「うちにブランドはない」は本当？／お金はかかる？／効果はいつ出る？
- 用語チップ：term-brand-identity / term-brand-image / term-brand-equity

### 02 vs-marketing｜マーケや広告と、何が違う？
- fig1：**レストランの断面図**——土台（ブランディング）の上にマーケ・広告・PRが載る層構造
- fig2：**時間軸の図**——開店前に決めること／開店後にやること
- タブ軸：**4つの言葉を比べる**（ブランディング／マーケティング／広告／PR。各タブ＝役割・時間軸・担当・答える問い）
- Q&A：広告費とブランド予算どちらが先？／マーケ部の仕事では？／SNS運用はブランディング？／両方いっぺんにできる？
- 用語チップ：term-positioning / term-concept / term-tone-voice

### 03 worldview｜世界観・“らしさ”はどう作る？
- fig1：**順番の逆転図**——「×見た目から始める」vs「○約束→振る舞い→見た目」
- fig2：**らしさの同心円**——ひとつの核（約束）から言葉・色・態度・価格が展開する図
- タブ軸：**Before / After**（順番を間違えた会社の道のり／核から作った会社の道のり）
- Q&A：センスがないと無理？／デザイナーに丸投げでいい？／途中で変えていい？／ガイドラインは必要？
- 用語チップ：term-concept / term-tone-voice / term-ci-vi-bi / term-brand-guidelines

### 04 does-it-work｜効果はある？どう測るの？
- fig1：**健康診断ダッシュボード**——5つの計器（知られているか・思い出されるか・指名されるか・値引きされないか・紹介されるか）
- fig2：**効果の時間差カーブ**——投資と効果の立ち上がりのズレを示す面グラフ
- タブ軸：**会社の段階別・見るべき数字**（創業期／成長期／成熟期）
- Q&A：売上に直結する？／何ヶ月で効く？／小さい会社でも測れる？／数字が悪かったらどうする？
- 用語チップ：term-brand-equity / term-brand-image
- 備考：診断CTAとの親和性が最も高いページ。§11のCTA文言をこのページだけ「いま、5つの計器を見てみる」に差し替え可

### 05 where-to-start｜何から、いつ始めればいい？
- fig1：**ステージ分岐マップ**——Build／Grow／Renew の三叉路図（situations/ 3ページと接続）
- fig2：**最初の一歩チェックリスト**——現在地を知る→一言で言う→接点を棚卸す、の3ステップ図
- タブ軸：**状況別・最初の一手**（0→1で作る／成長中に整える／既にあるものを刷新する。各タブ末尾に該当 situations ページへのリンク）
- Q&A：予算がない場合は？／社長一人でも始められる？／外部に頼むタイミングは？／まず何を書き出す？
- 用語チップ：term-mvv / term-positioning
- 前後ナビ：「次へ」は診断セクションへの誘導カードに置き換え

## 5. データ設計（basics.json の拡張スキーマ）

既存フィールド（slug/num/q/teaser/lead/sections/pull）は**そのまま維持**し、以下を追加する。

```jsonc
{
  "slug": "what-is-brand",
  // …既存フィールド…
  "answer": {                       // §2 30秒サマリー
    "one": "一文の答え（既存 pull を再利用してもよい）",
    "points": ["要点1", "要点2", "要点3"]
  },
  "figures": [                      // §4/§6 図解（差し替え可能画像）
    { "id": "fig1", "src": "assets/basics/diagrams/what-is-brand-fig1.svg",
      "alt": "説明", "caption": "図1 — キャプション" },
    { "id": "fig2", "src": "assets/basics/diagrams/what-is-brand-fig2.svg",
      "alt": "説明", "caption": "図2 — キャプション" }
  ],
  "tabs": {                         // §7 タブ
    "label": "立場で見る",
    "items": [ { "tab": "経営者から", "h": "小見出し", "p": ["段落…"] } ]
  },
  "faq": [ { "q": "質問", "a": "回答（1段落）" } ],   // §8
  "terms": ["term-brand-identity", "term-brand-image"] // §9（entries の slug）
}
```

- build-data.mjs の `renderBasicPage()` をこのスキーマ対応に改修。`figures` の src 実在チェック・`terms` の entries 解決チェックをビルド時バリデーションに追加。

## 6. 画像・図解の差し替え規約（あとから入れ替え可能にするため）

1. **ファイル置き場と命名**：`project/assets/basics/diagrams/<slug>-fig1.svg`（fig2 も同様）。この命名を守って**同名ファイルを上書きすれば、コード変更なしで全ページに反映**される。
2. **形式**：初期実装は Brandri パレットのSVG図解（Claudeが生成）。後日、写真・イラスト（PNG/JPG/WebP）に差し替える場合は basics.json の `src` の拡張子だけ変更する。
3. **キャンバス**：viewBox 1200×675（16:9固定）。差し替え画像も16:9で用意すると崩れない（コンポーネント側は aspect-ratio 16/9 + object-fit: contain で保険をかける）。
4. **図解内の文字**：日本語ラベルは可（アイコンと違い、図解は説明のための文字を許容）。ただし英字の全大文字は禁止。
5. **alt / caption**：basics.json で管理。画像を差し替えたら caption も合わせて更新する。

## 7. 実装ステップ（承認後）
1. basics.json に answer/figures/tabs/faq/terms を5ページぶん執筆・追加
2. 図解SVG 10枚（5ページ×2）を生成して diagrams/ に配置
3. renderBasicPage() を新テンプレートに改修（目次・タブ・アコーディオン・前後ナビ・バリデーション）
4. CSS追加（プログレスバー／サマリーカード／figure／タブ／details／用語チップ／前後ナビ、＋SP対応）
5. タブ用の最小JS（basicsページ専用、依存なし・JSオフでも全文閲覧可）
6. プレビュー検証（PC/SP・タブ・アコーディオン・アンカー・リンク解決）→ コミット → デプロイ → Artifact更新

## 8. 決めごと（確認事項）
- 本文4節は既存の文章を活かす（新規書き直しではなく、タブ・Q&A・図解ぶんを**追加執筆**する）
- 読了目安は文字数から自動計算（600字/分）
- JSON-LD は Article → **FAQPage を併記**（faq をそのまま構造化データ化、SEO加点）
